
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript PostGet.ts
  import { SDK } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.tags.postGet({
    id: "TAG-12345",
  });

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "tag": {
      "created_by": "string",
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": "string",
      "modified_date": "2023-01-01T12:00:00.000Z",
      "allowed_values": [
        "string"
      ],
      "description": "string",
      "name": "string"
    }
  }
  ```
  </CH.Code>
</WithStatusBar>

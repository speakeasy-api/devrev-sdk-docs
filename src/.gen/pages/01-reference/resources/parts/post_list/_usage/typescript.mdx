
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript PostList.ts
  import { ListMode, PartType, SDK } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.parts.postList({
    createdBy: [
      "DEVU-12345",
    ],
    name: [
      "string",
    ],
    ownedBy: [
      "DEVU-12345",
    ],
    parentPart: {
      parts: [
        "PROD-12345",
      ],
    },
    type: [
      PartType.Enhancement,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "next_cursor": "string",
    "parts": [
      "string"
    ],
    "prev_cursor": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>

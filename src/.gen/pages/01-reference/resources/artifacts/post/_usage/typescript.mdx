
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Post.ts
  import { SDK } from "devrev/sdk";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.artifacts.post({
    id: "ARTIFACT-12345",
    version: new Date("2023-01-01T12:00:00.000Z"),
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "expires_at": "2023-01-01T12:00:00.000Z",
    "url": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>

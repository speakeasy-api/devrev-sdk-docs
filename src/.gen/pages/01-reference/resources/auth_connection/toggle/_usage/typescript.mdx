```typescript Toggle.ts
import { AuthConnectionToggle, SDK } from "devrev/sdk";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.authConnection.toggle({
    id: "<ID>",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
```


import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "400"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```python Toggle.py
  import sdk

s = sdk.SDK(
    auth_token="",
)

req = sdk.DevOrgAuthConnectionsToggleRequest(
    id='<ID>',
)

res = s.auth_connection.toggle(req)

if res.status_code == 200:
    # handle response
    pass
  ```
  ---

  ```json Example Response
  {}
  ```
  </CH.Code>
</WithStatusBar>

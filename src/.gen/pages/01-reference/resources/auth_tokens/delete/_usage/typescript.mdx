```typescript Delete.ts
import { SDK } from "devrev/sdk";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.authTokens.delete({});

  if (res.statusCode == 200) {
    // handle response
  }
})();
```

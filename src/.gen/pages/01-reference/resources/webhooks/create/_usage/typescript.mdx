
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "201"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Create.ts
  import { SDK } from "openapi";
import { WebhookEventType } from "openapi/dist/models/components";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.webhooks.create({
    eventTypes: [
      WebhookEventType.WorkCreated,
    ],
    url: "http://whirlwind-diver.info",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "webhook": {
      "created_by": "string",
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": "string",
      "modified_date": "2023-01-01T12:00:00.000Z",
      "event_types": [],
      "secret": "string",
      "url": "string"
    }
  }
  ```
  </CH.Code>
</WithStatusBar>

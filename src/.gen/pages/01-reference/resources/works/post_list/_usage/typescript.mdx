
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript PostList.ts
  import { SDK } from "openapi";
import { IssuePriority, ListMode, TicketSeverity, WorkType } from "openapi/dist/models/components";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.works.postList({
    actualCloseDate: "string",
    appliesToPart: [
      "PROD-12345",
    ],
    createdBy: [
      "DEVU-12345",
    ],
    createdDate: "string",
    issue: {
      priority: [
        IssuePriority.P2,
      ],
      revOrgs: [
        "REV-AbCdEfGh",
      ],
    },
    modifiedDate: "string",
    ownedBy: [
      "DEVU-12345",
    ],
    stage: {
      name: [
        "string",
      ],
    },
    tags: [
      "TAG-12345",
    ],
    targetCloseDate: "string",
    ticket: {
      group: [
        "string",
      ],
      revOrg: [
        "REV-AbCdEfGh",
      ],
      severity: [
        TicketSeverity.Blocker,
      ],
      slaSummary: {},
      sourceChannel: [
        "string",
      ],
      surveys: {},
    },
    type: [
      WorkType.Issue,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "next_cursor": "string",
    "prev_cursor": "string",
    "works": [
      "string"
    ]
  }
  ```
  </CH.Code>
</WithStatusBar>

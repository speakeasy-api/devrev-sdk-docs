
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```python Delete.py
  import sdk
from sdk.models import components

s = sdk.SDK(
    auth_token="",
)

req = components.WorksDeleteRequest(
    id='ISS-12345',
)

res = s.works.delete(req)

if res.works_delete_response is not None:
    # handle response
    pass
  ```
  ---

  ```json Example Response
  {}
  ```
  </CH.Code>
</WithStatusBar>


import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "201"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Create.ts
  import { SDK } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.works.create("string");

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "work": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>


import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript List.ts
  import { SDK } from "openapi";
import { ListMode, TimelineEntryVisibility } from "openapi/dist/models/components";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.timelineEntries.list({
    object: "PROD-12345",
    visibility: [
      TimelineEntryVisibility.Internal,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "next_cursor": "string",
    "prev_cursor": "string",
    "timeline_entries": [
      "string"
    ]
  }
  ```
  </CH.Code>
</WithStatusBar>

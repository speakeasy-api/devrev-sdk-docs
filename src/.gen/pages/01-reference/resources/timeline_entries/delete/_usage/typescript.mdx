
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Delete.ts
  import { SDK } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.timelineEntries.delete({
    id: "don:core:<partition>:devo/<dev-org-id>:ticket/123:timeline_event/<timeline-event-id>",
  });

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {}
  ```
  </CH.Code>
</WithStatusBar>

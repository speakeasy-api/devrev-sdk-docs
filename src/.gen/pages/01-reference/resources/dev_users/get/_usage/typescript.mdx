
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Get.ts
  import { GetDevUsersRequest, SDK } from "devrev/sdk";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });
const id: string = "string";

  const res = await sdk.devUsers.get(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "dev_user": {
      "external_identities": [
        {}
      ],
      "created_by": "string",
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": "string",
      "modified_date": "2023-01-01T12:00:00.000Z",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "phone_numbers": [
        "string"
      ]
    }
  }
  ```
  </CH.Code>
</WithStatusBar>

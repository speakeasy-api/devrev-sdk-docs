
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Get.ts
  import { GetWorksRequest, SDK } from "devrev/sdk";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });
const id: string = "ISS-12345";

  const res = await sdk.works.get(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "work": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>

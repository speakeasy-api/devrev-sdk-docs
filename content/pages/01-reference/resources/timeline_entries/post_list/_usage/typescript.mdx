
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript PostList.ts
  import { ListMode, SDK, TimelineEntryVisibility } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.timelineEntries.postList({
    object: "PROD-12345",
    visibility: [
      TimelineEntryVisibility.Internal,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "next_cursor": "string",
    "prev_cursor": "string",
    "timeline_entries": [
      "string"
    ]
  }
  ```
  </CH.Code>
</WithStatusBar>


import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Get.ts
  import { SDK } from "openapi";
import { GetTimelineEntriesRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });
const id: string = "don:core:<partition>:devo/<dev-org-id>:ticket/123:timeline_event/<timeline-event-id>";

  const res = await sdk.timelineEntries.get(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "timeline_entry": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>

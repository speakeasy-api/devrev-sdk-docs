
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Get.ts
  import { SDK } from "openapi";
import { GetRevOrgsRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });
const account: string = "ACC-12345";
const id: string = "REV-AbCdEfGh";

  const res = await sdk.revOrgs.get(account, id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {
    "rev_org": {
      "description": "string",
      "domain": "string",
      "external_ref": "string",
      "created_by": "string",
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": "string",
      "modified_date": "2023-01-01T12:00:00.000Z",
      "display_name": "string"
    }
  }
  ```
  </CH.Code>
</WithStatusBar>

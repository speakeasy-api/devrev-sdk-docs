
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```python PostGet.py
  import sdk
from sdk.models import components

s = sdk.SDK(
    auth_token="",
)

req = components.DevOrgAuthConnectionsGetRequest(
    id='<ID>',
)

res = s.auth_connection.post_get(req)

if res.dev_org_auth_connections_get_response is not None:
    # handle response
    pass
  ```
  ---

  ```json Example Response
  {
    "auth_connection": {
      "display_name": "string",
      "enabled": false,
      "id": "string",
      "type": "samlp",
      "sign_in_endpoint": "string",
      "signing_cert": "string"
    }
  }
  ```
  </CH.Code>
</WithStatusBar>


import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Get.ts
  import { GetWebhooksRequest, SDK } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });
const id: string = "don:integration:<partition>:devo/<dev-org-id>:webhook/<webhook-id>";

  const res = await sdk.webhooks.get(id);

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "webhook": {
      "created_by": "string",
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": "string",
      "modified_date": "2023-01-01T12:00:00.000Z",
      "event_types": [],
      "secret": "string",
      "url": "string"
    }
  }
  ```
  </CH.Code>
</WithStatusBar>

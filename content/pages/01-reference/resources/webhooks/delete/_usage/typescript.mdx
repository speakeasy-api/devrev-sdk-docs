
import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript Delete.ts
  import { SDK } from "openapi";

(async() => {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.webhooks.delete({
    id: "don:integration:<partition>:devo/<dev-org-id>:webhook/<webhook-id>",
  });

  if (res.statusCode == 200) {
    // handle response
  }
})();
  ```
  ---

  ```json Example Response
  {}
  ```
  </CH.Code>
</WithStatusBar>

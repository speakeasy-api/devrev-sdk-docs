import OperationInfo from '/src/components/OperationInfo';

## Create

<OperationInfo method='post' path='/auth-tokens.create' />

Creates a JWT corresponding to the requested token type for the
authenticated user.



import WithStatusBar from '@/src/components/WithStatusBar';

<WithStatusBar statusItems={[["Status", "200"], ["Content-Type", "application/json"]]}>
  <CH.Code>
  ```typescript PostList.ts
  import { ListMode, SDK, UserState } from "devrev/sdk";

async function run() {
  const sdk = new SDK({
    authToken: "",
  });

  const res = await sdk.devUsers.postList({
    email: [
      "string",
    ],
    sortBy: [
      "string",
    ],
    state: [
      UserState.Locked,
    ],
  });

  if (res.statusCode == 200) {
    // handle response
  }
}

run();
  ```
  ---

  ```json Example Response
  {
    "dev_users": [
      {
        "external_identities": [
          {}
        ],
        "created_by": "string",
        "created_date": "2023-01-01T12:00:00.000Z",
        "display_id": "string",
        "id": "string",
        "modified_by": "string",
        "modified_date": "2023-01-01T12:00:00.000Z",
        "display_name": "string",
        "display_picture": {
          "display_id": "string",
          "id": "string"
        },
        "email": "string",
        "full_name": "string",
        "phone_numbers": [
          "string"
        ]
      }
    ],
    "next_cursor": "string",
    "prev_cursor": "string"
  }
  ```
  </CH.Code>
</WithStatusBar>
